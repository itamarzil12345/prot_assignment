apiVersion: apps/v1
kind: Deployment
metadata:
  name: protego-analysis
  namespace: default
  labels:
    app: protego-analysis
    service: analysis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: protego-analysis
  template:
    metadata:
      labels:
        app: protego-analysis
        service: analysis
    spec:
      containers:
      - name: analysis
        image: protego-analysis:latest
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: protego-config
        - secretRef:
            name: protego-secrets
        volumeMounts:
        - name: logs
          mountPath: /app/services/analysis/logs
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - python
            - -c
            - "import sys; sys.exit(0)"
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - python
            - -c
            - "import sys; sys.exit(0)"
          initialDelaySeconds: 10
          periodSeconds: 5
      volumes:
      - name: logs
        emptyDir: {}
      restartPolicy: Always

